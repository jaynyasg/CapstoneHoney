"""Root conftest for the CapstoneHoney repository.

The mere presence of this file puts the repository root on ``sys.path`` during
pytest collection, so tests can ``import detect.dp_honey`` without first
performing an editable install (``pip install -e .``).
"""
